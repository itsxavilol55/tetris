import './style.css'
const size = 20;
const columnas = 24;
const filas = 30;
const canvas = document.getElementById("canvas");
canvas.style.width = size * columnas + "px";
canvas.style.height = (size * filas) + "px";